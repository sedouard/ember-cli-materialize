<thead>
  <tr>
    {{#each columnComponents key="@index" as |column|}}
      {{view column.headerView column=column}}
    {{/each}}
  </tr>
</thead>
<tbody>
  {{#each content key="@index" as |row|}}
    <tr>
      {{yield row}}
    </tr>
  {{/each}}
</tbody>